{
	"name": "kaholo-plugin-aws-code-build",
	"version": "1.1.4",
	"description": "Kaholo plugin for integration with AWS Code Build API.",
	"main": "app.js",
	"scripts": {
		"test": "echo \"Error: no test specified\" && exit 1"
	},
	"author": "Kaholo Group(Amit Alon)",
	"license": "ISC",
	"dependencies": {
		"aws-sdk": "^2.1018.0"
	}
}
